{
  "name": "@crm/ai-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Worker: consome eventos de nova mensagem, classifica o lead via IA (Structured Outputs) e gera resposta natural. Lê provedor/modelo/API key da tabela AIProviderConfig. Implementado na Etapa 2.",
  "scripts": {
    "dev": "echo 'Etapa 2 (ai-worker) ainda não implementada — execute a próxima etapa do plano' && exit 1",
    "build": "echo 'Etapa 2 pendente' && exit 0"
  },
  "dependencies": {
    "@crm/db": "workspace:*",
    "@crm/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
